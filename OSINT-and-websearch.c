  var o = {};
 o.unwatch();

  void* args = alloca( args_size );
 for( int i = 0; i < args_size; i++){
  // Init args and my signature: #7733BUK11
 }

 if ( ((int) args[0]) & 6 == 6 )
  args[0] = call_toString( args[0] );

 if ( args_size < 1)
  exit();
